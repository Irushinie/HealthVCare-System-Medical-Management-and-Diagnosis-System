server:
  port: 9190
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/oauthHealtvcareDB?createDatabaseIfNotExist=true
    username: root
    password: Rootpassword@123
    driver-class-name: com.mysql.jdbc.Driver
    initialization-mode: always
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    org:
      hibernate:
        type: trace


